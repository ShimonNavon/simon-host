# Simon Host Favicon Implementation Plan

1. Add the approved external SVG favicon.
2. Replace the embedded favicon in the shared HTML head.
3. Run frontend tests and the production build.
4. Inspect generated HTML and the copied asset.
5. Commit, push `main`, wait for automatic deployment, and verify the live URL.

